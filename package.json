{
  "name": "albalon-request-match-",
  "version": "1.0.0",
  "description": "request-matches",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "npx nodemon --watch 'src/**/*.ts' --exec 'npm run build && node build/app.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "firebase": "^10.9.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "nodemon": "^3.1.0",
    "typescript": "^5.4.2"
  }
}
